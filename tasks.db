import sqlite3

conn = sqlite3.connect('tasks.db')
cursor = conn.cursor()

# Create tables for storing results
cursor.execute('''CREATE TABLE IF NOT EXISTS faq (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')
cursor.execute('''CREATE TABLE IF NOT EXISTS summaries (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')
cursor.execute('''CREATE TABLE IF NOT EXISTS journals (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')
cursor.execute('''CREATE TABLE IF NOT EXISTS ideas (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')
cursor.execute('''CREATE TABLE IF NOT EXISTS emails (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')
cursor.execute('''CREATE TABLE IF NOT EXISTS code_snippets (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')
cursor.execute('''CREATE TABLE IF NOT EXISTS blogs (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')
cursor.execute('''CREATE TABLE IF NOT EXISTS quizzes (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')
cursor.execute('''CREATE TABLE IF NOT EXISTS sentiments (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')
cursor.execute('''CREATE TABLE IF NOT EXISTS product_reviews (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')
cursor.execute('''CREATE TABLE IF NOT EXISTS courses (id INTEGER PRIMARY KEY, content TEXT, timestamp DATETIME)''')

conn.commit()
conn.close()
